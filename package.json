{
  "name": "gemini-completion-chime",
  "version": "6.0.0",
  "private": true,
  "description": "Chime on Gemini completion — FSM + Network detection",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.1.1",
    "jsdom": "^26.1.0"
  }
}
